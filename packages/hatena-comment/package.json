{
  "name": "hatena-comment",
  "main": "dist/index.js",
  "private": true,
  "scripts": {
    "build": "esbuild --minify --bundle src/index.ts --outfile=dist/index.js",
    "clear": "rm -rf dist/"
  }
}
